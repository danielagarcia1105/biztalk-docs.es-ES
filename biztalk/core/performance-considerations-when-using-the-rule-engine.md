---
title: Consideraciones sobre el rendimiento cuando se usa el motor de reglas | Documentos de Microsoft
ms.custom: 
ms.date: 06/08/2017
ms.prod: biztalk-server
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: 9e9020c2-5152-40f6-940b-d4ce4081f069
caps.latest.revision: "7"
author: MandiOhlinger
ms.author: mandia
manager: anneta
ms.openlocfilehash: c24a1c6ffb278d257e16c192e5fc7d827df70e24
ms.sourcegitcommit: cb908c540d8f1a692d01dc8f313e16cb4b4e696d
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/20/2017
---
# <a name="performance-considerations-when-using-the-rule-engine"></a><span data-ttu-id="c7dfe-102">Consideraciones de rendimiento al usar el motor de reglas</span><span class="sxs-lookup"><span data-stu-id="c7dfe-102">Performance Considerations When Using the Rule Engine</span></span>
<span data-ttu-id="c7dfe-103">Este tema trata sobre el rendimiento del motor de reglas en varios escenarios y con diferentes valores en los parámetros de configuración o ajuste.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-103">This topic discusses how the rule engine performs in various scenarios and with different values for the configuration/tuning parameters.</span></span>  
  
## <a name="fact-types"></a><span data-ttu-id="c7dfe-104">Tipos de hechos</span><span class="sxs-lookup"><span data-stu-id="c7dfe-104">Fact Types</span></span>  
 <span data-ttu-id="c7dfe-105">El motor de reglas tarda menos en obtener acceso a los hechos .NET que a los hechos XML y de base de datos.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-105">The rule engine takes less time to access .NET facts than it does to access XML and database facts.</span></span> <span data-ttu-id="c7dfe-106">Si puede elegir entre usar hechos .NET, XML o de base de datos en una directiva, debería optar más bien por usar hechos .NET para obtener un mejor rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-106">If you have a choice of using .NET, XML, or database facts in a policy, you should consider using .NET facts for better performance.</span></span>  
  
## <a name="data-table-vs-data-connection-binding"></a><span data-ttu-id="c7dfe-107">Vs de tabla de datos. Enlace de conexión de datos</span><span class="sxs-lookup"><span data-stu-id="c7dfe-107">Data Table vs. Data Connection Binding</span></span>  
 <span data-ttu-id="c7dfe-108">Cuando el tamaño del conjunto de datos es pequeño (inferior a unas 10 filas), el **TypedDataTable** enlace proporciona mejor rendimiento que la **DataConnection** enlace.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-108">When the size of the data set is small (fewer than approximately 10 rows), the **TypedDataTable** binding performs better than the **DataConnection** binding.</span></span> <span data-ttu-id="c7dfe-109">Cuando el conjunto de datos es grande (mayor o igual a aproximadamente 10 filas), el **DataConnection** enlace proporciona mejor rendimiento que la **TypedDataTable** enlace.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-109">When the data set is large (greater than or equal to approximately 10 rows), the **DataConnection** binding performs better than the **TypedDataTable** binding.</span></span> <span data-ttu-id="c7dfe-110">Por lo tanto, debe decidir si desea utilizar el **DataConnection** enlace o la **TypedDataTable** enlace en función del tamaño estimado del conjunto de datos.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-110">Therefore, you should decide whether to use the **DataConnection** binding or the **TypedDataTable** binding based on the estimated size of the data set.</span></span>  
  
## <a name="fact-retrievers"></a><span data-ttu-id="c7dfe-111">Administradores de almacenes de datos</span><span class="sxs-lookup"><span data-stu-id="c7dfe-111">Fact Retrievers</span></span>  
 <span data-ttu-id="c7dfe-112">Puede escribir un administrador de almacenes: un objeto que implementa métodos estándares y los utiliza para la fuente de alimentación a largo plazo y que cambian lentamente al motor de reglas antes de ejecutar la directiva.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-112">You can write a fact retriever—an object that implements standard methods and typically uses them to supply long-term and slowly changing facts to the rule engine before the policy is executed.</span></span> <span data-ttu-id="c7dfe-113">El motor almacena estos datos en caché y los utiliza en varios ciclos de ejecución.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-113">The engine caches these facts and uses them over multiple execution cycles.</span></span> <span data-ttu-id="c7dfe-114">En lugar de enviar un hecho estático o casi estático cada vez que invoque el motor de reglas, debería crear un administrador de almacenes de datos que envíe el hecho la primera vez y, a continuación, lo actualice en la memoria solo cuando se necesite.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-114">Instead of submitting a static or fairly static fact each time you invoke the rule engine, you should create a fact retriever that submits the fact the first time, and then updates the fact in memory only when it is needed.</span></span>  
  
## <a name="rule-priority"></a><span data-ttu-id="c7dfe-115">Prioridad de la regla</span><span class="sxs-lookup"><span data-stu-id="c7dfe-115">Rule Priority</span></span>  
 <span data-ttu-id="c7dfe-116">El valor de prioridad de una regla puede oscilar a ambos lados de **0**, con los números más grandes significan una mayor prioridad.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-116">The priority setting for a rule can range on either side of **0**, with larger numbers having higher priority.</span></span> <span data-ttu-id="c7dfe-117">Las acciones se ejecutan en orden desde prioridad más alta a prioridad más baja.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-117">Actions are executed in order from highest priority to lowest priority.</span></span> <span data-ttu-id="c7dfe-118">Cuando la directiva implementa un comportamiento de encadenamiento directo mediante el uso de **Assert/Update** llamadas, el encadenamiento se pueden optimizar mediante el uso de la configuración de prioridad.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-118">When the policy implements forward-chaining behavior by using **Assert/Update** calls, the chaining can be optimized by using the priority setting.</span></span> <span data-ttu-id="c7dfe-119">Por ejemplo, suponga que **Rule2** tiene una dependencia en un valor que se establece de forma **Rule1**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-119">For example, suppose that **Rule2** has a dependency on a value that is set by **Rule1**.</span></span> <span data-ttu-id="c7dfe-120">Dando **Rule1** una prioridad más alta significa que **Rule2** solo se ejecutará después **Rule1** se activa y actualiza el valor.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-120">Giving **Rule1** a higher priority means that **Rule2** will execute only after **Rule1** fires and updates the value.</span></span> <span data-ttu-id="c7dfe-121">Por el contrario, si **Rule2** es tiene una prioridad más alta, se puede desencadenar una vez y, a continuación, se activan después de volver a **Rule1** se activa y actualiza el hecho de que **Rule2** está usando en una condición.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-121">Conversely, if **Rule2** is given a higher priority, it can fire once, and then fire again after **Rule1** fires and updates the fact that **Rule2** is using in a condition.</span></span> <span data-ttu-id="c7dfe-122">Esto puede producir resultados correctos o no, pero está claro que una doble activación repercute más sobre el rendimiento que una sola.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-122">This may or may not produce the correct results, but clearly firing twice has a performance impact versus firing only once.</span></span>  
  
## <a name="update-calls"></a><span data-ttu-id="c7dfe-123">Llamadas a Update</span><span class="sxs-lookup"><span data-stu-id="c7dfe-123">Update Calls</span></span>  
 <span data-ttu-id="c7dfe-124">El **actualización** función actualiza un hecho de que existe en la memoria de trabajo del motor de reglas y hace que todas las reglas que usan el hecho actualizado en condiciones para volver a evaluar.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-124">The **Update** function updates a fact that exists in the working memory of the rule engine, and causes all the rules that use the updated fact in conditions to be reevaluated.</span></span> <span data-ttu-id="c7dfe-125">**Actualización** llamadas a funciones pueden ser costosas, especialmente si muchas reglas necesitan volver a evaluar debido a actualizaciones de hechos.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-125">**Update** function calls can be expensive, especially if many rules need to be reevaluated because of updating the facts.</span></span> <span data-ttu-id="c7dfe-126">Hay situaciones en las que es posible evitar que vuelvan a evaluarse las reglas.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-126">There are situations where you can avoid having to reevaluate the rules.</span></span> <span data-ttu-id="c7dfe-127">Por ejemplo, imagine las siguientes reglas:</span><span class="sxs-lookup"><span data-stu-id="c7dfe-127">For example, consider the following rules:</span></span>  
  
 <span data-ttu-id="c7dfe-128">**Rule1:**</span><span class="sxs-lookup"><span data-stu-id="c7dfe-128">**Rule1:**</span></span>  
  
```  
IF PurchaseOrder.Amount > 5   
THEN StatusObj.Flag = true; Update(StatusObj)  
```  
  
 <span data-ttu-id="c7dfe-129">**Rule2:**</span><span class="sxs-lookup"><span data-stu-id="c7dfe-129">**Rule2:**</span></span>  
  
```  
IF PurchaseOrder.Amount <= 5   
THEN StatusObj.Flag = false; Update(StatusObj)  
```  
  
 <span data-ttu-id="c7dfe-130">El resto de reglas de la directiva, utilice **StatusObj.Flag** en sus condiciones.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-130">All remaining rules of the policy use **StatusObj.Flag** in their conditions.</span></span> <span data-ttu-id="c7dfe-131">Por lo tanto, cuando **actualización** se llama en el **StatusObj** de objeto, se volverá a evaluar todas las reglas.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-131">Therefore, when **Update** is called on the **StatusObj** object, all the rules will be reevaluated.</span></span> <span data-ttu-id="c7dfe-132">Independientemente del valor de la **cantidad** campo es, todas las reglas excepto **Rule1** y **Rule2** se evalúan dos veces, una vez antes de la **actualización** llamada y una vez después de la **actualización** llamar.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-132">Whatever the value of the **Amount** field is, all the rules except **Rule1** and **Rule2** are evaluated twice, once before the **Update** call and once after the **Update** call.</span></span>  
  
 <span data-ttu-id="c7dfe-133">En su lugar, puede establecer el valor de la **marca** campo **false** antes de invocar la directiva y, a continuación, usar solo **Rule1** en la directiva para establecer la marca.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-133">Instead, you could set the value of the **flag** field to **false** before you invoke the policy and then use only **Rule1** in the policy to set the flag.</span></span> <span data-ttu-id="c7dfe-134">En este caso, **actualización** sólo se llama si el valor de la **cantidad** campo es mayor que 5, y **actualización** no se llama si la cantidad es menor o igual a 5.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-134">In this case, **Update** is called only if the value of the **Amount** field is greater than 5, and **Update** is not called if the amount is less than or equal to 5.</span></span> <span data-ttu-id="c7dfe-135">Por lo tanto, todas las reglas excepto **Rule1** y **Rule2** se evalúan dos veces solo si el valor de la **cantidad** campo es mayor que 5.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-135">Therefore, all the rules except **Rule1** and **Rule2** are evaluated twice only if the value of the **Amount** field is greater than 5.</span></span>  
  
## <a name="use-of-logical-or-operators"></a><span data-ttu-id="c7dfe-136">Uso de operadores O lógicos</span><span class="sxs-lookup"><span data-stu-id="c7dfe-136">Use of Logical OR Operators</span></span>  
 <span data-ttu-id="c7dfe-137">El motor de reglas está optimizado para ejecutar lógica **AND** operadores y reconstruye la regla que analizó en una forma disyuntiva normal hasta que **o** operador se usa solo en el nivel superior.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-137">The rule engine is optimized for executing logical **AND** operators and it reconstructs the rule it parsed in a disjunctive normal form so that **OR** operator is used only at the top level.</span></span> <span data-ttu-id="c7dfe-138">Con un número creciente de lógica **o** operadores en las condiciones crea más permutaciones que expanden la red de análisis del motor de reglas, y puede tardar mucho tiempo para el motor de reglas normalizar la regla.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-138">Using an increasing number of logical **OR** operators in conditions creates additional permutations that expand the analysis network of the rule engine, and It may take long time for the rule engine to normalize the rule.</span></span> <span data-ttu-id="c7dfe-139">La lista siguiente contiene posibles soluciones para este problema.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-139">The following list contains possible workarounds for this issue.</span></span>  
  
-   <span data-ttu-id="c7dfe-140">Modifique la regla para que sea la forma disyuntiva normal por lo que la **o** operador es sólo en el nivel superior.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-140">Modify the rule to be in disjunctive normal form so that the **OR** operator is only at the top level.</span></span> <span data-ttu-id="c7dfe-141">Tenga en cuenta que desarrollar una regla en forma disyuntiva normal en el Compositor de reglas de negocio puede ser complicado.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-141">Note that developing a rule in disjunctive normal form in Business Rule Composer can be tricky.</span></span> <span data-ttu-id="c7dfe-142">Quizás desee considerar la posibilidad de crear la regla mediante programación.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-142">You may want to consider creating the rule programmatically.</span></span>  
  
-   <span data-ttu-id="c7dfe-143">Desarrollar un componente de aplicación auxiliar que realiza la **o** las operaciones y devuelve un valor booleano y use el componente en la regla.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-143">Develop a helper component that performs the **OR** operations and returns a Boolean value, and use the component in the rule.</span></span>  
  
-   <span data-ttu-id="c7dfe-144">Considere la posibilidad de dividir la regla en varias y que una regla ejecutada previamente compruebe un conjunto de marcas o bien usar un objeto impuesto por una regla ejecutada previamente, tal como se muestra en los ejemplos siguientes:</span><span class="sxs-lookup"><span data-stu-id="c7dfe-144">Consider splitting the rule into multiple rules and have the rules check for a flag set by a previously executed rule or use an object that is asserted by a previously executed rule as shown in the following examples:</span></span>  
  
    -   <span data-ttu-id="c7dfe-145">Regla 1: IF (un == 1 o == 3), a continuación, b = true</span><span class="sxs-lookup"><span data-stu-id="c7dfe-145">Rule 1: IF (a == 1 OR a == 3) THEN b = true</span></span>  
  
         <span data-ttu-id="c7dfe-146">Regla 2: si (b == true), a continuación...</span><span class="sxs-lookup"><span data-stu-id="c7dfe-146">Rule 2: if (b == true) THEN …</span></span>  
  
    -   <span data-ttu-id="c7dfe-147">Regla 1: IF (un == 1 o == 3), a continuación, assert (nueva c())</span><span class="sxs-lookup"><span data-stu-id="c7dfe-147">Rule 1: IF (a == 1 OR a == 3) THEN assert(new c())</span></span>  
  
         <span data-ttu-id="c7dfe-148">Regla 2: IF (c.flag == true), a continuación...</span><span class="sxs-lookup"><span data-stu-id="c7dfe-148">Rule 2: IF (c.flag == true) THEN …</span></span>  
  
## <a name="caching-settings"></a><span data-ttu-id="c7dfe-149">Configuración de almacenamiento en caché</span><span class="sxs-lookup"><span data-stu-id="c7dfe-149">Caching Settings</span></span>  
 <span data-ttu-id="c7dfe-150">El motor de reglas usa dos cachés.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-150">The rule engine uses two caches.</span></span> <span data-ttu-id="c7dfe-151">La primera se encuentra en el servicio de actualización y la segunda en cada uno de los procesos de BizTalk.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-151">The first one is in the Update service and the second one is in each BizTalk process.</span></span> <span data-ttu-id="c7dfe-152">La primera vez que se usa una directiva, el proceso de BizTalk solicita la información de la directiva al servicio de actualización.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-152">The first time a policy is used, the BizTalk process requests the policy information from the Update service.</span></span> <span data-ttu-id="c7dfe-153">El servicio de actualización recupera la información de la directiva desde la base de datos del motor de reglas, la almacena en caché y devuelve la información al proceso de BizTalk.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-153">The Update service retrieves the policy information from the Rule Engine database, caches it, and returns the information to the BizTalk process.</span></span> <span data-ttu-id="c7dfe-154">El proceso de BizTalk crea un objeto de directiva basado en esa información y lo almacena en una caché cuando la instancia del motor de reglas completa la ejecución de la directiva.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-154">The BizTalk process creates a policy object based on that information and stores the policy object in a cache when the associated rule engine instance completes executing the policy.</span></span> <span data-ttu-id="c7dfe-155">Cuando se vuelve a invocar la misma directiva, el proceso de BizTalk usa nuevamente el objeto de la directiva desde la caché si hay alguno disponible.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-155">When the same policy is invoked again, the BizTalk process reuses the policy object from the cache if one is available in the cache.</span></span>  
  
 <span data-ttu-id="c7dfe-156">De igual modo, si el proceso de BizTalk solicita la información acerca de una directiva al servicio de actualización, éste busca en primer lugar la información de la directiva en su caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-156">Similarly, if the BizTalk process requests the information about a policy from the Update service, the Update service first looks for the policy information in its cache.</span></span> <span data-ttu-id="c7dfe-157">El servicio de actualización también comprueba cada 60 segundos si ha habido actualizaciones de la directiva que está en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-157">The Update service also checks every 60 seconds (one minute) to see if there have been any updates to the policy in the database.</span></span> <span data-ttu-id="c7dfe-158">Si hay actualizaciones, el servicio de actualización recupera la información y almacena en caché la información actualizada.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-158">If there are any updates, the Update service retrieves the information and caches the updated information.</span></span>  
  
 <span data-ttu-id="c7dfe-159">Hay tres parámetros de ajuste del motor de reglas relacionadas con estas cachés: **CacheEntries**, **Tiempodeesperadecaché**, y **PollingInterval**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-159">There are three tuning parameters for the rule engine related to these caches: **CacheEntries**, **CacheTimeout**, and **PollingInterval**.</span></span> <span data-ttu-id="c7dfe-160">Se pueden especificar los valores de estos parámetros tanto en el Registro como en un archivo de configuración.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-160">You can specify the values for these parameters either in the registry or in a configuration file.</span></span>  
  
 <span data-ttu-id="c7dfe-161">El valor de **CacheEntries** es el número máximo de entradas en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-161">The value of **CacheEntries** is the maximum number of entries in the cache.</span></span> <span data-ttu-id="c7dfe-162">El valor predeterminado de **CacheEntries** es 32.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-162">The default value of **CacheEntries** is 32.</span></span> <span data-ttu-id="c7dfe-163">Puede que desee aumentar el valor de la **CacheEntries** parámetro para mejorar el rendimiento en algunos casos.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-163">You may want to increase the value of the **CacheEntries** parameter to improve performance in some cases.</span></span> <span data-ttu-id="c7dfe-164">Por ejemplo, imagine que está usando 40 directivas de forma repetida.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-164">For example, suppose you are using 40 policies repeatedly.</span></span> <span data-ttu-id="c7dfe-165">En este caso puede que desee aumentar el valor de **CacheEntries** hasta 40 para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-165">In this case you may want to increase the value of **CacheEntries** to 40 to improve performance.</span></span> <span data-ttu-id="c7dfe-166">Con ello permitirá al servicio de actualización almacenar los detalles de hasta 40 directivas en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-166">This would allow the Update service to cache details of up to 40 policies in memory.</span></span> <span data-ttu-id="c7dfe-167">Y también hará que el servicio de BizTalk almacene hasta 40 instancias de directiva en la memoria caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-167">It would also cause the BizTalk service to cache up to 40 policy instances in memory.</span></span> <span data-ttu-id="c7dfe-168">Puede haber más de una instancia de una directiva en la caché del servicio de BizTalk.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-168">There may be more than one instance of a policy in the cache of the BizTalk service.</span></span>  
  
 <span data-ttu-id="c7dfe-169">El valor de **CacheTimeout** es el tiempo (en segundos) para que deje la memoria caché de servicio de actualización las entradas.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-169">The value of **CacheTimeout** is the time (in seconds) for entries to age out of the Update service cache.</span></span> <span data-ttu-id="c7dfe-170">En otras palabras, el **CacheTimeout** valor hace referencia a cuánto tiempo una entrada de caché para una directiva se conserva en la memoria caché si no hay ninguna referencia a ella.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-170">In other words, the **CacheTimeout** value refers to how long a cache entry for a policy is kept in the cache if there are no references to it.</span></span> <span data-ttu-id="c7dfe-171">El valor predeterminado de **CacheTimeout** es 3600 segundos (una hora).</span><span class="sxs-lookup"><span data-stu-id="c7dfe-171">The default value of **CacheTimeout** is 3600 seconds (one hour).</span></span> <span data-ttu-id="c7dfe-172">Esto quiere decir que, si no se hace referencia a la entrada de caché en el transcurso de una hora, se borrará.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-172">This means that, if the cache entry is not referenced within an hour, it is deleted.</span></span> <span data-ttu-id="c7dfe-173">En algunos casos, puede que desee aumentar el **CacheTimeout** valor para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-173">In some cases, you may want to increase the **CacheTimeout** value to improve performance.</span></span> <span data-ttu-id="c7dfe-174">Por ejemplo, suponga que se invoca la directiva cada dos horas.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-174">For example, suppose the policy is invoked every two hours.</span></span> <span data-ttu-id="c7dfe-175">Para mejorar el rendimiento de la ejecución de la directiva, aumentar el valor de la **CacheTimeout** parámetro con un valor superior a dos horas.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-175">You could improve the performance of the policy execution by increasing the value of the **CacheTimeout** parameter to a value greater than two hours.</span></span>  
  
 <span data-ttu-id="c7dfe-176">El **PollingInterval** parámetro para el motor de reglas define el tiempo en segundos para el intervalo en el que el servicio de actualización comprueba si la base de datos de motor de reglas para las actualizaciones.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-176">The **PollingInterval** parameter for the rule engine defines the time in seconds for the interval at which the Update service checks the Rule Engine database for updates.</span></span> <span data-ttu-id="c7dfe-177">El valor predeterminado para la **PollingInterval** parámetro es de 60 segundos (un minuto).</span><span class="sxs-lookup"><span data-stu-id="c7dfe-177">The default value for the **PollingInterval** parameter is 60 seconds (one minute).</span></span> <span data-ttu-id="c7dfe-178">Si sabe que las directivas no se actualizan o lo hacen con poca frecuencia, puede incrementar este valor para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-178">If you know that the policies do not get updated at all or are updated rarely, you could increase this value to improve performance.</span></span>  
  
## <a name="sideeffects-property"></a><span data-ttu-id="c7dfe-179">SideEffects (propiedad)</span><span class="sxs-lookup"><span data-stu-id="c7dfe-179">SideEffects Property</span></span>  
 <span data-ttu-id="c7dfe-180">El **ClassMemberBinding**, **DatabaseColumnBinding**, y **XmlDocumentFieldBinding** las clases tienen una propiedad denominada **SideEffects**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-180">The **ClassMemberBinding**, **DatabaseColumnBinding**, and **XmlDocumentFieldBinding** classes have a property named **SideEffects**.</span></span> <span data-ttu-id="c7dfe-181">Esta propiedad determina si el valor del campo, de la columna o del miembro enlazados se ha almacenado en caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-181">This property determines whether the value of the bound field, member, or column is cached.</span></span> <span data-ttu-id="c7dfe-182">El valor predeterminado de la **SideEffects** propiedad en el **DatabaseColumnBinding** y **XmlDocumentFieldBinding** las clases es **false**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-182">The default value of the **SideEffects** property in the **DatabaseColumnBinding** and **XmlDocumentFieldBinding** classes is **false**.</span></span> <span data-ttu-id="c7dfe-183">El valor predeterminado de la **SideEffects** propiedad en el **ClassMemberBinding** clase es **true**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-183">The default value of the **SideEffects** property in the **ClassMemberBinding** class is **true**.</span></span> <span data-ttu-id="c7dfe-184">Por lo tanto, cuando se obtiene acceso por segunda vez (o más tarde dentro de la directiva) a un campo de un documento XML o a una columna de una tabla de una base de datos, su valor se recupera desde la caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-184">Therefore, when a field of an XML document or a column of a database table is accessed for the second time or later within the policy, its value is retrieved from the cache.</span></span> <span data-ttu-id="c7dfe-185">Sin embargo, cuando se obtiene acceso por segunda vez (o posteriormente) a un miembro de un objeto .NET, el valor se recupera desde el objeto .NET y no desde la caché.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-185">However, when a member of a .NET object is accessed for the second time or later, the value is retrieved from the .NET object, and not from the cache.</span></span> <span data-ttu-id="c7dfe-186">Establecer el **SideEffects** propiedad de .NET **ClassMemberBinding** a **false** mejorará el rendimiento porque el valor del campo se recupera de la caché desde el segunda vez.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-186">Setting the **SideEffects** property of a .NET **ClassMemberBinding** to **false** will improve performance because the value of the field is retrieved from the cache from the second time onwards.</span></span> <span data-ttu-id="c7dfe-187">Tan solo se puede hacer esto mediante programación.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-187">You can only do this programmatically.</span></span> <span data-ttu-id="c7dfe-188">La herramienta de Compositor de reglas de negocio no expone el **SideEffects** propiedad.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-188">The Business Rule Composer tool does not expose the **SideEffects** property.</span></span>  
  
## <a name="instances-and-selectivity"></a><span data-ttu-id="c7dfe-189">Instances y Selectivity</span><span class="sxs-lookup"><span data-stu-id="c7dfe-189">Instances and Selectivity</span></span>  
 <span data-ttu-id="c7dfe-190">El **clases XmlDocumentBinding**, **ClassBinding**, y **DatabaseBinding** tienen dos propiedades: **instancias** y **Selectividad**.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-190">The **XmlDocumentBinding**, **ClassBinding**, and **DatabaseBinding** classes have two properties: **Instances** and **Selectivity**.</span></span> <span data-ttu-id="c7dfe-191">El valor de **instancias** es el número esperado de instancias de la clase en la memoria de trabajo.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-191">The value of **Instances** is the expected number of instances of the class in working memory.</span></span> <span data-ttu-id="c7dfe-192">El valor de **selectividad** es el porcentaje de las instancias de clase que satisfarán las condiciones de regla.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-192">The value of **Selectivity** is the percentage of the class instances that will successfully pass the rule conditions.</span></span> <span data-ttu-id="c7dfe-193">El motor de reglas usa estos valores para optimizar la evaluación de condiciones de modo que se utilice primero el menor número de instancias posible en las evaluaciones de condiciones y posteriormente se usen las instancias restantes.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-193">The rule engine uses these values to optimize the condition evaluation so that the fewest possible instances are used in condition evaluations first and then the remaining instances are used.</span></span> <span data-ttu-id="c7dfe-194">Si tiene un conocimiento previo del número de instancias del objeto, al establecer el **instancias** ese valor para propiedad mejoraría el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-194">If you have prior knowledge of the number of instances of the object, setting the **Instances** property to that value would improve performance.</span></span> <span data-ttu-id="c7dfe-195">De forma similar, si tiene un conocimiento previo del porcentaje de los objetos que satisfacen las condiciones, al establecer el **selectividad** ese valor para propiedad mejoraría el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-195">Similarly, if you have prior knowledge of the percentage of these objects passing the conditions, setting the **Selectivity** property to that value would improve performance.</span></span> <span data-ttu-id="c7dfe-196">Tan solo se pueden establecer los valores de estos parámetros mediante programación.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-196">You can only set values for these parameters programmatically.</span></span> <span data-ttu-id="c7dfe-197">La herramienta Compositor de reglas de negocio no los expone.</span><span class="sxs-lookup"><span data-stu-id="c7dfe-197">The Business Rule Composer tool does not expose them.</span></span>