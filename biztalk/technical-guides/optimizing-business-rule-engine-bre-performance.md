---
title: Optimizar el rendimiento del motor (BRE) de regla de negocios | Documentos de Microsoft
ms.custom: ''
ms.date: 06/08/2017
ms.prod: biztalk-server
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
ms.assetid: c271b059-174d-4e8b-88b5-c3f408a97f1f
caps.latest.revision: 4
author: MandiOhlinger
ms.author: mandia
manager: anneta
ms.openlocfilehash: 14c3adf32ac06d80c1aab8f870d82156470097a5
ms.sourcegitcommit: cb908c540d8f1a692d01dc8f313e16cb4b4e696d
ms.translationtype: MT
ms.contentlocale: es-ES
ms.lasthandoff: 09/20/2017
ms.locfileid: "22298932"
---
# <a name="optimizing-business-rule-engine-bre-performance"></a><span data-ttu-id="417f8-102">Optimizar el rendimiento del motor (BRE) de regla de negocios</span><span class="sxs-lookup"><span data-stu-id="417f8-102">Optimizing Business Rule Engine (BRE) Performance</span></span>
<span data-ttu-id="417f8-103">Al implementar el motor de reglas de negocios (BRE) en una solución de BizTalk Server, deben tener en cuenta los siguientes factores:</span><span class="sxs-lookup"><span data-stu-id="417f8-103">The following factors should be considered when implementing the Business Rule Engine (BRE) in a BizTalk Server solution:</span></span>  
  
## <a name="fact-types"></a><span data-ttu-id="417f8-104">Tipos de hechos</span><span class="sxs-lookup"><span data-stu-id="417f8-104">Fact types</span></span>  
 <span data-ttu-id="417f8-105">El motor de reglas tarda menos tiempo en hechos de .NET de un acceso en comparación con el tiempo que se tarda en hechos XML y la base de datos de access.</span><span class="sxs-lookup"><span data-stu-id="417f8-105">The rule engine takes less time to access .NET facts compared to the time it takes to access XML and database facts.</span></span> <span data-ttu-id="417f8-106">Si tiene una opción de usar hechos .NET o XML o base de datos en una directiva, considere la posibilidad de usar hechos .NET para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-106">If you have a choice of using either .NET or XML or database facts in a policy, you should consider using .NET facts for improved performance.</span></span>  
  
## <a name="data-table-vs-data-connection"></a><span data-ttu-id="417f8-107">Tabla de datos frente a conexión de datos</span><span class="sxs-lookup"><span data-stu-id="417f8-107">Data table vs. data connection</span></span>  
 <span data-ttu-id="417f8-108">Cuando el tamaño del conjunto de datos es pequeño (< 10 o lo), el **TypedDataTable** enlace proporciona un mejor rendimiento que la **DataConnection** enlace.</span><span class="sxs-lookup"><span data-stu-id="417f8-108">When the size of the data set is small (< 10 or so), the **TypedDataTable** binding provides better performance than the **DataConnection** binding.</span></span> <span data-ttu-id="417f8-109">Sin embargo, el **DataConnection** enlace proporciona mejor rendimiento que la **TypedDataTable** enlace cuando el conjunto de datos es grande (mayor o igual a 10 filas aproximadamente).</span><span class="sxs-lookup"><span data-stu-id="417f8-109">However, the **DataConnection** binding performs better than the **TypedDataTable** binding when the data set is large (greater than or equal to 10 rows approximately).</span></span> <span data-ttu-id="417f8-110">Por lo tanto, debe decidir si desea utilizar el **DataConnection** enlace o **TypedDataTable** enlace en función del tamaño estimado del conjunto de datos.</span><span class="sxs-lookup"><span data-stu-id="417f8-110">Therefore, you should decide whether to use the **DataConnection** binding or **TypedDataTable** binding based on the estimated size of the data set.</span></span>  
  
## <a name="fact-retrievers"></a><span data-ttu-id="417f8-111">Almacenes de datos</span><span class="sxs-lookup"><span data-stu-id="417f8-111">Fact retrievers</span></span>  
 <span data-ttu-id="417f8-112">Un administrador de almacenes implementa métodos estándar que se usan normalmente para proporcionar los hechos a largo plazo y que cambian lentamente al motor de reglas antes de ejecutar una directiva.</span><span class="sxs-lookup"><span data-stu-id="417f8-112">A fact retriever implements standard methods which are typically used to supply long-term and slowly changing facts to the rule engine before a policy is executed.</span></span> <span data-ttu-id="417f8-113">El motor almacena estos datos en caché y los utiliza en varios ciclos de ejecución.</span><span class="sxs-lookup"><span data-stu-id="417f8-113">The engine caches these facts and uses them over multiple execution cycles.</span></span> <span data-ttu-id="417f8-114">En lugar de enviar un hecho estático o relativamente estático cada vez que se invoca el motor de reglas, debe crear un administrador de almacenes que envíe el hecho por primera vez y, a continuación, actualiza los hechos en memoria solo cuando sea necesario.</span><span class="sxs-lookup"><span data-stu-id="417f8-114">Instead of submitting a static or fairly static fact each time that you invoke the rule engine, you should create a fact retriever that submits the fact for the first time, and then updates the fact in memory only when necessary.</span></span>  
  
## <a name="rule-priority"></a><span data-ttu-id="417f8-115">Prioridad de la regla</span><span class="sxs-lookup"><span data-stu-id="417f8-115">Rule priority</span></span>  
 <span data-ttu-id="417f8-116">El valor de prioridad de una regla puede oscilar a ambos lados de **0**, con los números más grandes significan una mayor prioridad.</span><span class="sxs-lookup"><span data-stu-id="417f8-116">The priority setting for a rule can range on either side of **0**, with larger numbers having higher priority.</span></span> <span data-ttu-id="417f8-117">Las acciones se ejecutan en orden de prioridad más alta a prioridad más baja.</span><span class="sxs-lookup"><span data-stu-id="417f8-117">Actions are executed in order from the highest priority to lowest priority.</span></span> <span data-ttu-id="417f8-118">Cuando la directiva implementa un comportamiento de encadenamiento directo mediante el uso de **Assert/Update** llamadas, el encadenamiento se pueden optimizar mediante el uso de la configuración de prioridad.</span><span class="sxs-lookup"><span data-stu-id="417f8-118">When the policy implements forward-chaining behavior by using **Assert/Update** calls, the chaining can be optimized by using the priority setting.</span></span> <span data-ttu-id="417f8-119">Por ejemplo, supongamos que **Rule2** tiene una dependencia en un valor establecido por **Rule1**.</span><span class="sxs-lookup"><span data-stu-id="417f8-119">For example, assume that **Rule2** has a dependency on a value set by **Rule1**.</span></span> <span data-ttu-id="417f8-120">Dando **Rule1** una prioridad más alta significa que **Rule2** sólo se ejecutarán después de **Regla1** se activa y actualiza el valor.</span><span class="sxs-lookup"><span data-stu-id="417f8-120">Giving **Rule1** a higher priority means that **Rule2** will only execute after **Rule1** fires and updates the value.</span></span> <span data-ttu-id="417f8-121">Por el contrario, si **Rule2** estaban tiene una prioridad más alta, podría activarse una vez y, a continuación, se activan después de volver a **Rule1** se activa y actualice el hecho de que **Rule2** está usando una condición.</span><span class="sxs-lookup"><span data-stu-id="417f8-121">Conversely, if **Rule2** were given a higher priority, it could fire once, and then fire again after **Rule1** fires and update the fact that **Rule2** is using a condition.</span></span> <span data-ttu-id="417f8-122">Aunque esto puede proporcionar un resultado correcto, lo que proporciona **Rule1** una prioridad más alta en este escenario le proporcionará un mejor rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-122">While this may provide a correct result, giving **Rule1** a higher priority in this scenario will provide better performance.</span></span>  
  
## <a name="update-calls"></a><span data-ttu-id="417f8-123">Llamadas de actualización</span><span class="sxs-lookup"><span data-stu-id="417f8-123">Update calls</span></span>  
 <span data-ttu-id="417f8-124">La función de actualización hace que todas las reglas mediante los hechos actualizados para volver a evaluar.</span><span class="sxs-lookup"><span data-stu-id="417f8-124">The Update function causes all the rules using the updated facts to be reevaluated.</span></span> <span data-ttu-id="417f8-125">Llamadas a funciones de actualización pueden resultar costosas, especialmente si se vuelve a evaluar en un gran conjunto de reglas al actualizar los hechos.</span><span class="sxs-lookup"><span data-stu-id="417f8-125">Update function calls can be expensive especially if a large set of rules is reevaluated when updating facts.</span></span> <span data-ttu-id="417f8-126">Hay situaciones en las que se puede evitar este comportamiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-126">There are situations where this behavior can be avoided.</span></span> <span data-ttu-id="417f8-127">Por ejemplo, considere las siguientes reglas.</span><span class="sxs-lookup"><span data-stu-id="417f8-127">For example, consider the following rules.</span></span>  
  
 <span data-ttu-id="417f8-128">**Rule1:**</span><span class="sxs-lookup"><span data-stu-id="417f8-128">**Rule1:**</span></span>  
  
```  
IF PurchaseOrder.Amount > 5   
THEN StatusObj.Flag = true; Update(StatusObj)  
```  
  
 <span data-ttu-id="417f8-129">**Rule2:**</span><span class="sxs-lookup"><span data-stu-id="417f8-129">**Rule2:**</span></span>  
  
```  
IF PurchaseOrder.Amount <= 5   
THEN StatusObj.Flag = false; Update(StatusObj)  
```  
  
 <span data-ttu-id="417f8-130">El resto de reglas de la directiva, utilice **StatusObj.Flag** en sus condiciones.</span><span class="sxs-lookup"><span data-stu-id="417f8-130">All remaining rules of the policy use **StatusObj.Flag** in their conditions.</span></span> <span data-ttu-id="417f8-131">Por lo tanto, cuando **actualización** se llama en el **StatusObj** de objeto, se volverá a evaluar todas las reglas.</span><span class="sxs-lookup"><span data-stu-id="417f8-131">Therefore, when **Update** is called on the **StatusObj** object, all rules will be reevaluated.</span></span> <span data-ttu-id="417f8-132">Independientemente del valor de la **cantidad** campo es, todas las reglas excepto **Rule1** o **Rule2** se evalúan dos veces, una vez antes de la **actualización** llamar a y una vez después de la **actualización** llamar.</span><span class="sxs-lookup"><span data-stu-id="417f8-132">Whatever the value of the **Amount** field is, all rules except **Rule1** or **Rule2** are evaluated twice, once before the **Update** call and once after the **Update** call.</span></span>  
  
 <span data-ttu-id="417f8-133">Para mitigar asociado sobrecarga, podría establecer el valor de la **marca** campo **false** antes de invocar la directiva y, a continuación, use solo **Rule1** en la directiva para establecer la marca .</span><span class="sxs-lookup"><span data-stu-id="417f8-133">To mitigate the associated overhead, you could set the value of the **flag** field to **false** prior to invoking the policy and then use only **Rule1** in the policy to set the flag.</span></span> <span data-ttu-id="417f8-134">En este caso, **actualización** se denominaría sólo si el valor de la **cantidad** campo es mayor que 5 y el **actualización** función no se invoca si el valor de  **Cantidad** es menor o igual que 5.</span><span class="sxs-lookup"><span data-stu-id="417f8-134">In this case, **Update** would be called only if the value of the **Amount** field is greater than 5, and the **Update** function is not called if the value of **Amount** is less than or equal to 5.</span></span> <span data-ttu-id="417f8-135">Por lo tanto, todas las reglas excepto **Rule1** o **Rule2** se evalúan dos veces solo si el valor de la **cantidad** campo es mayor que 5.</span><span class="sxs-lookup"><span data-stu-id="417f8-135">Therefore, all the rules except **Rule1** or **Rule2** are evaluated twice only if the value of the **Amount** field is greater than 5.</span></span>  
  
## <a name="usage-of-logical-or-operators"></a><span data-ttu-id="417f8-136">Uso de operadores lógicos o</span><span class="sxs-lookup"><span data-stu-id="417f8-136">Usage of logical OR operators</span></span>  
 <span data-ttu-id="417f8-137">El uso de un número cada vez mayor de operadores lógicos O en las condiciones crea permutaciones adicionales que expanden la red de análisis del motor de reglas.</span><span class="sxs-lookup"><span data-stu-id="417f8-137">Using an increasing number of logical OR operators in conditions creates additional permutations that expand the analysis network of the rule engine.</span></span> <span data-ttu-id="417f8-138">Desde el punto de vista del rendimiento, será mejor dividir las condiciones en reglas atómicas que no contengan operadores lógicos O .</span><span class="sxs-lookup"><span data-stu-id="417f8-138">From a performance standpoint, you are better off splitting the conditions into atomic rules that do not contain logical OR operators.</span></span>  
  
## <a name="caching-settings"></a><span data-ttu-id="417f8-139">Configuración de almacenamiento en caché</span><span class="sxs-lookup"><span data-stu-id="417f8-139">Caching settings</span></span>  
 <span data-ttu-id="417f8-140">El motor de reglas usa dos cachés.</span><span class="sxs-lookup"><span data-stu-id="417f8-140">The Rule Engine uses two caches.</span></span> <span data-ttu-id="417f8-141">La primera de ellas es utilizada por el servicio de actualización y la otra se usa por cada proceso de BizTalk.</span><span class="sxs-lookup"><span data-stu-id="417f8-141">The first one is used by the update service and the second one is used by each BizTalk process.</span></span> <span data-ttu-id="417f8-142">La primera vez que se usa una directiva, el proceso de BizTalk solicita la información de directiva desde el servicio de actualización.</span><span class="sxs-lookup"><span data-stu-id="417f8-142">The first time a policy is used, the BizTalk process requests the policy information from the update service.</span></span> <span data-ttu-id="417f8-143">El servicio de actualización recupera la información de directiva de la base de datos de motor de reglas, lo almacena en caché y devuelve la información para el proceso de BizTalk.</span><span class="sxs-lookup"><span data-stu-id="417f8-143">The update service retrieves the policy information from the rule engine database, caches it and returns the information to the BizTalk process.</span></span> <span data-ttu-id="417f8-144">El proceso de BizTalk crea un objeto de directiva basado en esa información y almacena el objeto de directiva en una memoria caché cuando la instancia de motor de reglas asociado completa la ejecución de la directiva.</span><span class="sxs-lookup"><span data-stu-id="417f8-144">The BizTalk process creates a policy object based on that information and stores the policy object in a cache when the associated rule engine instance completes execution of the policy.</span></span> <span data-ttu-id="417f8-145">Cuando se vuelve a invocar la misma directiva, el proceso de BizTalk vuelve a utilizar el objeto de directiva de la memoria caché si está disponible.</span><span class="sxs-lookup"><span data-stu-id="417f8-145">When the same policy is invoked again, the BizTalk process reuses the policy object from the cache if one is available.</span></span> <span data-ttu-id="417f8-146">De forma similar, si el proceso de BizTalk solicita información acerca de una directiva de servicio de actualización, el servicio de actualización busca la información de directiva en su caché si está disponible.</span><span class="sxs-lookup"><span data-stu-id="417f8-146">Similarly, if the BizTalk process requests information about a policy from update service, the update service looks for the policy information in its cache if it is available.</span></span> <span data-ttu-id="417f8-147">Cada 60 segundos, el servicio de actualización también comprueba si se han producido las actualizaciones de la directiva en la base de datos.</span><span class="sxs-lookup"><span data-stu-id="417f8-147">Every 60 seconds, the update service also checks if there have been any updates to the policy in the database.</span></span> <span data-ttu-id="417f8-148">Si hay actualizaciones, el servicio de actualización recupera la información y almacena en caché la información actualizada.</span><span class="sxs-lookup"><span data-stu-id="417f8-148">If there are any updates, the update service retrieves the information and caches the updated information.</span></span>  
  
 <span data-ttu-id="417f8-149">Hay tres parámetros de ajuste del motor de reglas relacionadas con estas cachés: **CacheEntries**, **Tiempodeesperadecaché**, y **PollingInterval**.</span><span class="sxs-lookup"><span data-stu-id="417f8-149">There are three tuning parameters for the rule engine related to these caches: **CacheEntries**, **CacheTimeout**, and **PollingInterval**.</span></span> <span data-ttu-id="417f8-150">Se pueden especificar los valores de estos parámetros tanto en el Registro como en un archivo de configuración.</span><span class="sxs-lookup"><span data-stu-id="417f8-150">You can specify the values for these parameters either in the registry or in a configuration file.</span></span> <span data-ttu-id="417f8-151">El valor de la **CacheEntries** parámetro es el número máximo de entradas en la caché y se establece en un valor de 32 de forma predeterminada.</span><span class="sxs-lookup"><span data-stu-id="417f8-151">The value of the **CacheEntries** parameter is the maximum number of entries in the cache and is set to a value of 32 by default.</span></span> <span data-ttu-id="417f8-152">Puede que desee aumentar el valor de la **CacheEntries** parámetro para mejorar el rendimiento en ciertos escenarios.</span><span class="sxs-lookup"><span data-stu-id="417f8-152">You may want to increase the value of the **CacheEntries** parameter to improve performance in certain scenarios.</span></span> <span data-ttu-id="417f8-153">Por ejemplo, suponga que está usando 40 directivas repetidamente; se puede realizar para aumentar el valor de la **CacheEntries** parámetro hasta 40 para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-153">For example, say you are using 40 policies repeatedly; you could to increase the value of the **CacheEntries** parameter to 40 to improve performance.</span></span> <span data-ttu-id="417f8-154">Esto permitiría que el servicio de actualización mantener los detalles de la memoria caché de hasta 40 directivas en la memoria.</span><span class="sxs-lookup"><span data-stu-id="417f8-154">This would allow the update service to maintain cache details of up to 40 policies in memory.</span></span>  
  
 <span data-ttu-id="417f8-155">El valor de **CacheTimeout** es el tiempo en segundos que se mantiene una entrada en la caché del servicio de actualización.</span><span class="sxs-lookup"><span data-stu-id="417f8-155">The value of **CacheTimeout** is the time in seconds that an entry is maintained in the update service cache.</span></span> <span data-ttu-id="417f8-156">En otras palabras, el **CacheTimeout** valor hace referencia a cuánto tiempo una entrada de caché para una directiva se mantiene en la memoria caché sin que se hace referencia.</span><span class="sxs-lookup"><span data-stu-id="417f8-156">In other words, the **CacheTimeout** value refers to how long a cache entry for a policy is maintained in the cache without being referenced.</span></span> <span data-ttu-id="417f8-157">El valor predeterminado de **CacheTimeout** parámetro es 3600 segundos, o 1 hora.</span><span class="sxs-lookup"><span data-stu-id="417f8-157">The default value of **CacheTimeout** parameter is 3600 seconds, or 1 hour.</span></span> <span data-ttu-id="417f8-158">Significa que si la entrada de caché no se hace referencia en una hora, se elimina la entrada.</span><span class="sxs-lookup"><span data-stu-id="417f8-158">It means that if the cache entry is not referenced within an hour, the entry is deleted.</span></span> <span data-ttu-id="417f8-159">En algunos casos, puede ser beneficioso aumentar el valor de la **CacheTimeout** parámetro para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-159">In some cases, it may be beneficial to increase the value of the **CacheTimeout** parameter to improve performance.</span></span> <span data-ttu-id="417f8-160">Por ejemplo, si una directiva se invoca cada dos horas, de la ejecución de la directiva se puede mejorar el rendimiento aumentando el **CacheTimeout** parámetro con un valor superior a dos horas.</span><span class="sxs-lookup"><span data-stu-id="417f8-160">For example, if a policy is invoked every two hours, performance of the policy execution would be improved by increasing the **CacheTimeout** parameter to a value higher than two hours.</span></span>  
  
 <span data-ttu-id="417f8-161">El **PollingInterval** parámetro del motor de reglas define el tiempo en segundos para el servicio de actualización para comprobar la base de datos de motor de reglas para las actualizaciones.</span><span class="sxs-lookup"><span data-stu-id="417f8-161">The **PollingInterval** parameter of the rule engine defines the time in seconds for the update service to check the rule engine database for updates.</span></span> <span data-ttu-id="417f8-162">El valor predeterminado para la **PollingInterval** parámetro es de 60 segundos.</span><span class="sxs-lookup"><span data-stu-id="417f8-162">The default value for the **PollingInterval** parameter is 60 seconds.</span></span> <span data-ttu-id="417f8-163">Si sabe que las directivas no se actualizan en absoluto o se actualizan con poca frecuencia, puede cambiar este parámetro en un valor mayor para mejorar el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-163">If you know that the policies do not get updated at all or are updated rarely, you could change this parameter to a higher value to improve performance.</span></span>  
  
## <a name="sideeffects-property"></a><span data-ttu-id="417f8-164">Propiedad SideEffects</span><span class="sxs-lookup"><span data-stu-id="417f8-164">SideEffects property</span></span>  
 <span data-ttu-id="417f8-165">El **ClassMemberBinding**, **DatabaseColumnBinding**, y **XmlDocumentFieldBinding** las clases tienen una propiedad denominada **SideEffects**.</span><span class="sxs-lookup"><span data-stu-id="417f8-165">The **ClassMemberBinding**, **DatabaseColumnBinding**, and **XmlDocumentFieldBinding** classes have a property named **SideEffects**.</span></span> <span data-ttu-id="417f8-166">Esta propiedad determina si el valor del campo, de la columna o del miembro enlazados se ha almacenado en caché.</span><span class="sxs-lookup"><span data-stu-id="417f8-166">This property determines whether the value of the bound field, member, or column is cached.</span></span> <span data-ttu-id="417f8-167">El valor predeterminado de la **SideEffects** propiedad en el **DatabaseColumnBinding** y **XmlDocumentFieldBinding** las clases es **false**.</span><span class="sxs-lookup"><span data-stu-id="417f8-167">The default value of the **SideEffects** property in the **DatabaseColumnBinding** and **XmlDocumentFieldBinding** classes is **false**.</span></span> <span data-ttu-id="417f8-168">El valor predeterminado de la **SideEffects** propiedad en el **ClassMemberBinding** clase es **true**.</span><span class="sxs-lookup"><span data-stu-id="417f8-168">The default value of the **SideEffects** property in the **ClassMemberBinding** class is **true**.</span></span> <span data-ttu-id="417f8-169">Por lo tanto, cuando se obtiene acceso por segunda vez (o más tarde dentro de la directiva) a un campo de un documento XML o a una columna de una tabla de una base de datos, su valor se recupera desde la caché.</span><span class="sxs-lookup"><span data-stu-id="417f8-169">Therefore, when a field of an XML document or a column of a database table is accessed for the second time or later within the policy, its value is retrieved from the cache.</span></span> <span data-ttu-id="417f8-170">Sin embargo, cuando se obtiene acceso por segunda vez (o posteriormente) a un miembro de un objeto .NET, el valor se recupera desde el objeto .NET y no desde la caché.</span><span class="sxs-lookup"><span data-stu-id="417f8-170">However, when a member of a .NET object is accessed for the second time or later, the value is retrieved from the .NET object, and not from the cache.</span></span> <span data-ttu-id="417f8-171">Establecer el **SideEffects** propiedad de .NET **ClassMemberBinding** a **false** mejorará el rendimiento porque el valor del campo se recupera de la caché desde el segunda vez.</span><span class="sxs-lookup"><span data-stu-id="417f8-171">Setting the **SideEffects** property of a .NET **ClassMemberBinding** to **false** will improve performance because the value of the field is retrieved from the cache from the second time onwards.</span></span> <span data-ttu-id="417f8-172">Tan solo se puede hacer esto mediante programación.</span><span class="sxs-lookup"><span data-stu-id="417f8-172">You can only do this programmatically.</span></span> <span data-ttu-id="417f8-173">La herramienta de Compositor de reglas de negocio no expone el **SideEffects** propiedad.</span><span class="sxs-lookup"><span data-stu-id="417f8-173">The Business Rule Composer tool does not expose the **SideEffects** property.</span></span>  
  
## <a name="instances-and-selectivity"></a><span data-ttu-id="417f8-174">Instances y selectivity</span><span class="sxs-lookup"><span data-stu-id="417f8-174">Instances and selectivity</span></span>  
 <span data-ttu-id="417f8-175">El **clases XmlDocumentBinding**, **ClassBinding**, y **DatabaseBinding** tienen dos propiedades: **instancias** y **Selectividad**.</span><span class="sxs-lookup"><span data-stu-id="417f8-175">The **XmlDocumentBinding**, **ClassBinding**, and **DatabaseBinding** classes have two properties: **Instances** and **Selectivity**.</span></span> <span data-ttu-id="417f8-176">El valor de Instances corresponde al número esperado de instancias de la clase en la memoria de trabajo.</span><span class="sxs-lookup"><span data-stu-id="417f8-176">The value of Instances is the expected number of instances of the class in working memory.</span></span> <span data-ttu-id="417f8-177">El valor de **selectividad** es el porcentaje de las instancias de clase que satisfarán las condiciones de regla.</span><span class="sxs-lookup"><span data-stu-id="417f8-177">The value of **Selectivity** is the percentage of the class instances that will successfully pass the rule conditions.</span></span> <span data-ttu-id="417f8-178">El motor de reglas usa estos valores para optimizar la evaluación de condiciones de modo que se utilice primero el menor número de instancias posible en las evaluaciones de condiciones y posteriormente se usen las instancias restantes.</span><span class="sxs-lookup"><span data-stu-id="417f8-178">The rule engine uses these values to optimize the condition evaluation so that the fewest possible instances are used in condition evaluations first and then the remaining instances are used.</span></span> <span data-ttu-id="417f8-179">Si tiene un conocimiento previo del número de instancias del objeto, al establecer el **instancias** ese valor para propiedad mejoraría el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-179">If you have prior knowledge of the number of instances of the object, setting the **Instances** property to that value would improve performance.</span></span> <span data-ttu-id="417f8-180">De forma similar, si tiene un conocimiento previo del porcentaje de los objetos que satisfacen las condiciones, al establecer el **selectividad** ese valor para propiedad mejoraría el rendimiento.</span><span class="sxs-lookup"><span data-stu-id="417f8-180">Similarly, if you have prior knowledge of the percentage of these objects passing the conditions, setting the **Selectivity** property to that value would improve performance.</span></span> <span data-ttu-id="417f8-181">Tan solo se pueden establecer los valores de estos parámetros mediante programación.</span><span class="sxs-lookup"><span data-stu-id="417f8-181">You can only set values for these parameters programmatically.</span></span> <span data-ttu-id="417f8-182">La herramienta Compositor de reglas de negocio no los expone.</span><span class="sxs-lookup"><span data-stu-id="417f8-182">The Business Rule Composer tool does not expose them.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="417f8-183">Vea también</span><span class="sxs-lookup"><span data-stu-id="417f8-183">See Also</span></span>  
 [<span data-ttu-id="417f8-184">Optimizar el rendimiento de BizTalk Server</span><span class="sxs-lookup"><span data-stu-id="417f8-184">Optimizing BizTalk Server Performance</span></span>](../technical-guides/optimizing-biztalk-server-performance.md)